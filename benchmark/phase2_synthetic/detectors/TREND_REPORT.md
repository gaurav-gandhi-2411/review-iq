# Phase 2 Trend Detector -- Flagged Products

> SYNTHETIC-VALIDATED DETECTOR -- see trend.py module docstring. NOT proven against real
> seller data. STALE: this file was generated against a nearly-empty partial extractions.jsonl
> (~15-31/837 real extractions), not a meaningful run. Real interim validation (keyword-proxy,
> full 837-review coverage) shows 1.0 precision / 1.0 recall / 0.0 FPR -- see
> project_phase2_synthetic_testbed.md memory and the consolidated report. Re-run
> `uv run python trend.py` once extractions.jsonl has full/adequate real coverage to regenerate
> this file with real numbers.

Moderation-prioritization signal only -- confidence + evidence for human review, never an automated verdict that a product is in crisis.

Total flagged (product, topic) pairs: 0 (stale partial-data run, not authoritative)
