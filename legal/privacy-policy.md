> **⚠️ REQUIRES LEGAL REVIEW.** This document was drafted by an AI coding assistant from the
> product's actual technical implementation and standard SaaS legal templates. It is a
> starting draft, not legal advice, and must be reviewed by qualified counsel before being
> relied upon or published to customers.

# Privacy Policy

**Product:** Samidha Reviews ("the Service"). **Operator:** Gaurav Gandhi ("we", "us", "our").
**Effective date:** `[TODO: GG to set on publication]`

This Policy explains what data the Service collects, how it is processed, where it goes, how
long it is kept, and what rights you (or, if you are a business customer, your own end-customers
whose review text you submit) have over it. It applies to the hosted Service at
`api.samidhareviews.xyz` (production Cloud Run host as of this drafting:
`review-iq-ajjrytb3na-el.a.run.app`) and the account/signup surfaces.

**Two categories of person this Policy speaks to:**

- **Customer** — the business/organization that signs up for an API key and sends review text to
  the Service.
- **End-customer** — the person who wrote a review that a Customer submits to the Service (e.g. a
  Customer's own shopper). If review text contains an end-customer's personal data (name, email,
  phone number, order ID), the Customer is the **data controller** for that data, and we act as
  the Customer's **data processor** — see the [DPA template](./dpa-template.md).

---

## 1. What data we collect

| Category | Examples | Source |
|---|---|---|
| **Review text** | The raw text submitted to `/v2/extract`, `/v2/ingest/csv`, `/demo/extract`, etc. May contain end-customer PII (names, emails, phone numbers, order/invoice IDs) if the Customer's own review data includes it. | Submitted by the Customer via the API |
| **Structured extraction output** | Sentiment, topics, pros/cons, competitor mentions, urgency signal, authenticity score/label/flags. | Generated by the Service from review text |
| **Account/signup data** | Email address (via Supabase magic-link auth), organization slug, API key metadata (prefix, quota, creation/revocation timestamps — never the raw key itself after initial display). | Provided at signup |
| **Usage/API logs** | Request timestamps, endpoint, response status, latency, `org_id`, structured log fields (`latency_ms`, `provider`, `model`, `tokens_in`, `tokens_out` where applicable). Raw review text is **not** written to structured logs beyond what's needed to serve the request. | Generated by the Service on each call |
| **Authenticity audit records** | `org_id`, a SHA-256 hash of the review text (**not** the plaintext), score, label, flags, timestamp. | Generated when authenticity scoring runs (`authenticity_audits` table) — see `SECURITY.md` §10, `docs/compliance.md` |
| **Corrections** | User-submitted field corrections to extraction output, scoped to an allow-listed set of fields. | Submitted via `POST /v2/corrections` |

We do not collect payment information as of this drafting — no paid tier is enabled and no
payment processor is integrated (see `docs/cost-model.md`, which plans for a ~3% processing fee
+ India GST but does not commit to a specific processor). This section will be updated with the
processor's name and its own data-handling terms before any paid tier goes live.

---

## 2. How data is processed

1. **PII redaction before egress.** Review text is passed through a sanitizer
   (`app/core/sanitize.py`) before being sent to any LLM provider. As implemented today, the
   sanitizer performs **destructive redaction** — it replaces detected emails, Indian and common
   international phone number formats, and credit card numbers with `[REDACTED]` placeholders.
   This is a one-way redaction, not a reversible tokenization scheme (a reversible token map is a
   stated future goal in the Wave 1 spec but is **not yet implemented** — this Policy describes
   current behavior only). `[TODO: update this section if/when reversible tokenization ships.]`
2. **LLM extraction.** The (redacted) review text is sent to **Groq** (primary inference
   provider; current production models are `openai/gpt-oss-20b` and `openai/gpt-oss-120b`,
   routed by review length/complexity) to generate the structured extraction and authenticity
   signal. See [`sub-processors.md`](./sub-processors.md) for what Groq receives and Groq's own
   data-handling commitments, now independently verified against Groq's own Services Agreement
   (see that document for the exact clause). If Groq is unavailable, requests fail over to
   **OpenRouter** (a secondary provider), which is instructed, on every request, to route only
   to endpoints on OpenRouter's live Zero-Data-Retention allowlist — see
   [`sub-processors.md`](./sub-processors.md) for how this is enforced and independently
   verified. Both providers are gated by a `trains_on_input` check that unconditionally blocks
   any provider that trains on customer input (`assert_privacy_safe()` in `app/core/llm.py` —
   see `SECURITY.md` §3). Google Gemini is excluded from the customer-data (`/v2`) path
   entirely.
3. **Storage.** Extraction output, authenticity audit records (hashed review text only, not
   plaintext — see §1 table above), account data, and usage records are stored in **Supabase
   Postgres**, isolated per organization via Row-Level Security (RLS) — see
   `docs/data-ownership.md` for the isolation model and [`sub-processors.md`](./sub-processors.md)
   for what Supabase, as our database/auth sub-processor, has access to.
4. **No cross-tenant use.** No Customer's data is pooled into an aggregate corpus, shared model,
   or cross-tenant analysis without that Customer's explicit, revocable opt-in — see
   `docs/data-ownership.md` §"Cross-tenant data use" for the (currently unimplemented) consent
   design that would gate any such future use.

---

## 3. Retention

Samidha Reviews has **two retention modes, chosen per organization. Stateless is the default.**

- **Stateless (default).** Submitted review text and extraction output are never written to our
  database or application logs — the response you receive is the only copy. Usage counters and
  per-call cost records still persist (they never contain review content), for billing and
  quota purposes.
- **Retained (opt-in).** An organization that wants the dashboard, review search, or trend
  insights opts into retained mode and picks a window — **30 or 90 days.** Data older than the
  window is purged automatically; you may also purge your stored data on demand, at any time,
  without waiting for the window.
- **Bulk CSV upload requires retained mode** — bulk rows are staged durably so an interrupted
  upload can resume, which is not compatible with the stateless guarantee.

See [`data-retention-and-deletion.md`](./data-retention-and-deletion.md) for the full schedule
and every deletion/purge mechanism. In summary:

- **On self-service account deletion:** deletion is immediate and permanent in the primary
  database (a single cascading transaction — see §"The real deletion mechanism" in the linked
  document). Encrypted nightly database backups may retain a copy of deleted data for up to 90
  days (the current backup-artifact retention ceiling) before that backup itself expires.
- `[TODO: GG-DECISION / REQUIRES-LEGAL-REVIEW — whether 90-day backup residency after a deletion
  request is acceptable disclosure under DPDP/GDPR is a legal call; the retained-mode window
  choice (30/90 days) is a genuine product decision already made (D1/D2), not an infrastructure
  default like the backup number is.]`

---

## 4. International data transfer

- **Groq** (LLM inference) is a US-headquartered company. Verified directly against Groq's own
  Services Agreement (§4.2, `console.groq.com/docs/legal/services-agreement`): Groq is
  contractually barred from using submitted Inputs/Outputs to train or fine-tune any model
  unless explicitly authorized, does not retain Inputs/Outputs beyond what's needed to serve the
  request (temporary logs, up to 30 days, for reliability/abuse monitoring only, with a
  self-serve Zero Data Retention opt-out), and this Agreement applies to free-tier/self-serve API
  use with no Order Form required — the same terms this project's usage is governed by. `[TODO:
  the specific data-center region(s) Groq uses to physically serve inference were not
  independently verified — the contractual no-training/limited-retention commitment above is
  confirmed, but geographic data-residency specifically is not. Verify before publishing if
  region-specific transfer language is needed.]`
- **OpenRouter** (secondary/failover LLM inference, US-based aggregator). Verified live
  (2026-07-31, re-confirmed 2026-09-12): every request from this integration carries
  `provider.zdr=true`, restricting routing to OpenRouter's published Zero-Data-Retention
  endpoint allowlist; a real end-to-end call was independently cross-checked against that live
  allowlist and confirmed to have routed to a listed ZDR endpoint. See
  [`sub-processors.md`](./sub-processors.md) for the full mechanism.
- **Supabase** (database/auth) — for this specific project, the production Postgres connection is
  hosted on AWS in the `ap-south-1` (Mumbai, India) region, confirmed from this project's own
  connection configuration (`ops/runbooks/connection-modes.md`: pooler host
  `aws-0-ap-south-1.pooler.supabase.com`). This means Customer and end-customer data stored via
  Supabase for this Service resides in India. `[TODO: verify this against Supabase's own
  published infrastructure/subprocessor documentation before publishing, since project-level
  region selection can differ from Supabase's broader multi-region infrastructure — see
  sub-processors.md for the URL to check.]`
- **Google Cloud Run** (our own compute hosting, not a sub-processor of Customer data in the
  GDPR sense — see [`sub-processors.md`](./sub-processors.md) for why) runs in `asia-south1`
  (Mumbai, India), confirmed from `ops/runbooks/cloud-run-deploy.md`.
- **Backups.** Nightly encrypted database backup artifacts are stored via GitHub Actions, which
  Github's own documentation states are hosted in US data centers (per
  `ops/runbooks/db-restore.md`'s own "Geo-redundancy" note). This means an encrypted copy of
  Customer/end-customer data transits through and is retained in the US for up to 90 days via
  this backup mechanism, independent of where the live database is hosted.

If you are an EU-based Customer (or process EU end-customer data), the above constitutes our
current international-transfer footprint. `[TODO: REQUIRES-LEGAL-REVIEW — whether Standard
Contractual Clauses or another GDPR Art. 44+ transfer mechanism is needed for the
US-based legs (Groq, GitHub backups) is a legal determination, not an engineering one.]`

---

## 5. Your rights

Depending on applicable law (GDPR for EU-connected data, DPDP Act 2023 for India-connected data —
see [`compliance-posture.md`](./compliance-posture.md)), you may have the right to:

- **Access** the data we hold about your account and your organization's stored review data —
  available today via `GET /v2/dataset` and `GET /v2/dataset/export?format=jsonl`
  (`docs/data-ownership.md`).
- **Delete** your account and all associated data — available today via the self-service
  `DELETE /account` flow (type-to-confirm), or purge just your stored review data (retained-mode
  organizations) on demand via `POST /v2/purge` without deleting the account itself — see
  [`data-retention-and-deletion.md`](./data-retention-and-deletion.md).
- **Port** your data — the JSONL export above is designed to be a stable, self-describing format
  for this purpose.
- **Correct** extraction output you believe is wrong — via `POST /v2/corrections`
  (`docs/data-ownership.md` §"Corrections discipline").
- **Object to or restrict** certain processing, where applicable law grants that right.

**If you are an end-customer** (i.e. you wrote a review that a Samidha Reviews Customer submitted
to the Service, and you are not yourself a Samidha Reviews account holder): requests about your
personal data should generally be directed to the Customer (the business you reviewed), who is
the data controller. We support Customers in fulfilling such requests as their processor — see
the [DPA template](./dpa-template.md).

To exercise any of the above rights directly against us (e.g. as an account holder), contact:
`privacy@samidhareviews.xyz` — see also [`compliance-posture.md`](./compliance-posture.md)'s
DPDP grievance officer requirement, which is a related but distinct contact (same mailbox,
different regulatory role).

---

## 6. Security measures

See `SECURITY.md` in the repository for the full, current technical security posture (PII
redaction, prompt-injection defenses, argon2id API key hashing, Postgres RLS tenant isolation,
quota enforcement, secret management). A summary relevant to this Policy:

- Review text is redacted for common PII patterns before being sent to any LLM provider.
- All tenant data is isolated at the database layer via Row-Level Security, not application logic
  alone.
- API keys are never stored in plaintext (argon2id hashes only).
- Secrets are held in Google Cloud Secret Manager, not in source code or environment files.

---

## 7. Children's data

The Service is intended for business use (Customers submitting their own end-customers' review
data) and is not directed at children. We do not knowingly collect personal data directly from
children. If review text submitted by a Customer happens to identify a minor (e.g. a product
review mentioning a child), that data is handled under the same controller/processor terms as any
other end-customer data in the [DPA template](./dpa-template.md).

---

## 8. Changes to this Policy

We may update this Policy as the Service evolves. Material changes will be reflected by an
updated "Effective date" above.

---

## 9. Contact

- **Privacy/data-rights requests:** `privacy@samidhareviews.xyz`
- **Security disclosures:** see `SECURITY.md` (`gaurav.gandhi2411@gmail.com`).
- **DPDP grievance officer (India):** see [`compliance-posture.md`](./compliance-posture.md).

---

_This document is version-controlled; see git history for changes. Originally drafted 2026-07-31;
updated 2026-09-12 for the two-mode retention model, current Groq/OpenRouter facts (verified live
this session), and current production model names. **The two-mode retention model described in
§3 is implemented and tested but not yet deployed to production** (PR #167, pending a schema
migration) — remove this note once that PR has merged and the migration is applied._
